# AliPay-F2F-For-WHMCS

基于Alipay即时支付，手机支付，当面付的基于WHMCS的支付网关模块

支持 WHMCS 5.x/6.x/7.x/8.x

**请注意不是每一项都需要配置的！**

因为 WHMCS8.x（我测试的是最新款8.1.0）引入了数据库内容加密，直接读取数据库的内容读出来的是一串乱码（应该是仅和站点有关，与保存时间无关，怀疑是为了最小化数据泄露可能带来的影响所以引入了加密），不能再使用直接读取数据库数据的方案来处理了。

link_gen.php则因为调用时会传入参数，所以直接引入参数即可，无需再读取数据库。
